bag_name: "going_forward"
description: "this bag is goint ot show how we drive forward mostly honestly. not sure yet what else what we are doing when going this way but this will definitely work"
format: "sqlite"
compression:
  compression_format: "Zstd"
  compression_mode: "file"
max_bag_length: 100000
topics:
 - "/rosout"